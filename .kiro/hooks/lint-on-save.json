{
  "name": "Lint on Save",
  "version": "1.0.0",
  "description": "Run type check when Python files are saved",
  "when": {
    "type": "fileEdited",
    "patterns": ["src/**/*.py"]
  },
  "then": {
    "type": "runCommand",
    "command": "python -m py_compile ${file}"
  }
}
